# Git

## Command

### add
- change to staged
```
git add .
git add path/to/file
```

### commit 
- commit
```
git commit -m "commit message"
```

### revert sourve
- revert single file
```
git checkout {commit_id} -- path/to/file
```
